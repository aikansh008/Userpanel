import 'package:flutter/material.dart';

class TeacherAttendence extends StatelessWidget {
  const TeacherAttendence({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}